<%= form_for([:admin, @user], html: { class: 'form-horizontal' }) do |f| %>
	<fieldset>
		<legend>
			<%= @user.new_record?? 'New' : 'Editing' %> admin_user
		</legend>
		<div class="control-group">
			<%= f.label :email, class: 'control-label' %>
			<div class="controls">
				<%= f.text_field :email %>
			</div>
		</div>
		<div class="form-actions">
			<%= f.submit class: 'btn btn-primary' %>
		</div>
	</fieldset>
<% end %>